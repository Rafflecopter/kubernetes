{% set etcd_endpoints = "" -%}
{% if grains.etcd_servers is defined -%}
  {% set etcd_endpoints = grains.etcd_servers -%}
{% endif -%}

# test_args has to be kept at the end, so they'll overwrite any prior configuration
DAEMON_ARGS="-etcd-endpoints={{etcd_endpoints}} -v=2"
